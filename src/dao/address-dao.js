/**
 * All the functions to read, create, update and delete an address
 * @param {Action to perform} action 
 * @param {Parameters} params 
 * @param {On which page is it used} context 
 */
export default function address(action) {
  if(action === 'addAddressToCart') {
    //this method has been moved to cart-dao
  } 
}